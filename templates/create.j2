{% extends "base.j2" %}

{% block body %}
<div class="flex flex-col items-center justify-start h-auto px-8 py-6 sm:px-16 md:px-28">
    <form action="{{url_for('create')}}" class='flex flex-col items-center justify-normal' method="post">
        {{form.hidden_tag()}}
        {{form.text(cols=80, rows=20, class='post-area')}}
        {{form.submit(class="btn-submit")}}
    </form>
</div>
{% endblock body %}